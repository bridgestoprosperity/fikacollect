import 'package:flutter/material.dart';

class OtherSurvey extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Other Report Survey'),
      ),
      body: Center(
        child: Text('Other Survey Form Here'),
      ),
    );
  }
}