import 'package:flutter/material.dart';

class WaterSurvey extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Water Report Survey'),
      ),
      body: Center(
        child: Text('Water Survey Form Here'),
      ),
    );
  }
}